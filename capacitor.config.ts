import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.icumosque.salat ',
	appName: 'micusalat',
	webDir: 'dist',
	server: {
		androidScheme: 'https',
	},
};

export default config;
